language: node_js
node_js:
  - "0.10"
services:
  - selenium
addons:
  firefox: "17.0"
script:
  - sudo npm install
 # - export DISPLAY=:99.0
  - xvfb-run -e /dev/stdout --server-args="-ac" npm test test
cache: true
