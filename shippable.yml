language: node_js

node_js:
  - "0.10"
  
build_image: drydock/u14nod:prod

addons:
  firefox: "17.0"

services:
  - selenium

script:
  - sudo npm install
  - export DISPLAY=:99.0
  - xvfb-run --server-args="-ac" npm test
